# 🏠 Homelab Cost Optimizer

[![License: Apache-2.0](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](LICENSE)
[![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-green.svg)](https://www.python.org/)
[![CI](https://github.com/ranas-mukminov/homelab-cost-optimizer/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/homelab-cost-optimizer/actions/workflows/ci.yml)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

---

## Описание

**Homelab Cost Optimizer** — это комплексный инструмент управления инфраструктурой для энтузиастов домашних лабораторий и команд малого бизнеса. Он объединяет готовые к использованию шаблоны Infrastructure-as-Code (IaC) с интеллектуальным движком оптимизации затрат, который анализирует энергопотребление, утилизацию ресурсов и расходы на инфраструктуру на различных платформах виртуализации.

Этот проект помогает системным администраторам Linux, DevOps-инженерам и инфраструктурным командам снизить операционные расходы, стандартизировать развёртывания и поддерживать эффективную инфраструктуру домашней лаборатории или малого бизнеса без ручного учёта в таблицах.

## Основные возможности

- 🏗️ **Готовые IaC-шаблоны** — Terraform и Ansible шаблоны для типовых стеков (Proxmox, K3s, NAS, VPN)
- 📊 **Сбор данных с нескольких платформ** — Автоматический сбор метрик из Proxmox, libvirt, Docker и Kubernetes API
- ⚡ **Оценка энергопотребления** — Расчёт затрат на электроэнергию на основе настраиваемых профилей мощности и локальных тарифов
- 💰 **Движок анализа затрат** — Отслеживание ежемесячных расходов на инфраструктуру и выявление возможностей оптимизации
- 🔄 **Рекомендации по консолидации** — Предложения с использованием ИИ для объединения низко загруженных рабочих нагрузок и сокращения активных узлов
- 📝 **Автоматическая отчётность** — Генерация отчётов в формате Markdown или с использованием ИИ с практическими рекомендациями по экономии
- 🤖 **Интеграция с ИИ** — Опциональная интеграция с OpenAI для интеллектуальных предложений по шаблонам и нарративных отчётов
- 🔒 **Безопасность по умолчанию** — Без хранения учётных данных, API-ключи через переменные окружения, комплексное сканирование безопасности

## Архитектура и компоненты

Проект состоит из двух основных слоёв, работающих совместно:

### Инфраструктурный слой (blueprints/)

```
Модули Terraform ──┐
Плейбуки Ansible ──┼──> Воспроизводимые стеки домашних лабораторий
Определения K3d ───┘     (Proxmox, K3s, NAS, VPN, мониторинг)
```

- **Terraform**: Разворачивание инфраструктуры (VM, сети, хранилище)
- **Ansible**: Управление конфигурацией (укрепление ОС, развёртывание сервисов)
- **K3d**: Лёгкий Kubernetes для сред разработки

### Слой оптимизации (optimizer/)

```
Сбор данных ──> Нормализация ──> Анализ ──> Рекомендации
     |               |              |            |
Proxmox API     Модели Node/   Power/Cost   Сценарии
libvirt API     Workload       Estimators  консолидации
Docker API                                      |
Kubernetes API                          Markdown/AI отчёты
```

**Поток данных:**
1. Коллекторы получают метрики ресурсов через инфраструктурные API
2. Унифицированные модели нормализуют данные с различных платформ
3. Оценщики рассчитывают энергопотребление и затраты
4. Консолидаторы моделируют сценарии оптимизации
5. Генераторы отчётов создают практические рекомендации

## Требования

### Операционная система
- Linux (Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux 8+)
- Любой современный дистрибутив Linux с поддержкой Python 3.10+

### Доступ к инфраструктуре
- **Для шаблонов**: Terraform 1.5+, Ansible 2.14+
- **Для оптимизатора**: API-доступ хотя бы к одной платформе:
  - Proxmox VE 7.0+ (требуется API-токен)
  - libvirt (доступ к сокету)
  - Docker Engine (доступ к сокету)
  - Кластер Kubernetes (kubeconfig)

### Системные ресурсы
- **CPU**: рекомендуется 2+ ядра
- **RAM**: минимум 2GB, рекомендуется 4GB
- **Диск**: 500MB для приложения, дополнительное место для шаблонов
- **Сеть**: доступ в Интернет для установки пакетов и опциональных ИИ-функций

### Зависимости Python
- Python 3.10 или выше
- менеджер пакетов pip
- Виртуальное окружение (рекомендуется)

### Опциональные компоненты
- **API-ключ OpenAI** — для отчётов на основе ИИ и помощи с шаблонами
- **k3d** — для локальной разработки Kubernetes (v5.4.0+)
- **Proxmox VE** — для виртуализации домашней лаборатории

## Быстрый старт (TL;DR)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/ranas-mukminov/homelab-cost-optimizer
cd homelab-cost-optimizer

# 2. Установить оптимизатор
python3 -m venv venv
source venv/bin/activate
pip install -e .

# 3. Настроить цены на электроэнергию
cp config/electricity.example.yaml config/electricity.yaml
# Отредактировать config/electricity.yaml с вашим тарифом

# 4. Собрать метрики с вашей инфраструктуры
homelab-cost-optimizer collect \
  --source proxmox \
  --url https://pve.example.local:8006 \
  --token-id user@pam!tokenname \
  --token-secret ВАШ_ТОКЕН_СЕКРЕТ \
  --out inventory.json

# 5. Проанализировать затраты
homelab-cost-optimizer analyze \
  --input inventory.json \
  --config config/electricity.yaml \
  --out report.md

# 6. Получить предложения по консолидации
homelab-cost-optimizer suggest \
  --scenario consolidate-low-util \
  --input inventory.json \
  --config config/optimizer.yaml
```

**Доступ по умолчанию**: отчёты о затратах сохраняются как Markdown-файлы в текущей директории.

## Детальная установка

### Установка на Ubuntu / Debian

```bash
# Обновить индекс пакетов
sudo apt update

# Установить Python 3.10+ и pip
sudo apt install python3 python3-pip python3-venv git -y

# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/homelab-cost-optimizer
cd homelab-cost-optimizer

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить пакет в режиме разработки
pip install -e .

# Установить инструменты разработки (опционально)
pip install -e .[dev]

# Проверить установку
homelab-cost-optimizer --version
```

### Установка на RHEL / Rocky / AlmaLinux

```bash
# Установить Python 3.10+ и зависимости
sudo dnf install python3.10 python3.10-pip git -y

# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/homelab-cost-optimizer
cd homelab-cost-optimizer

# Создать виртуальное окружение
python3.10 -m venv venv
source venv/bin/activate

# Установить пакет
pip install -e .

# Проверить установку
homelab-cost-optimizer --version
```

### Установка с Docker / Docker Compose

```bash
# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/homelab-cost-optimizer
cd homelab-cost-optimizer

# Собрать контейнер (пример Dockerfile не включён в репозиторий)
# Примечание: запуск напрямую с установкой Python обеспечивает лучшую совместимость
```

**Примечание**: проект разработан для прямого запуска на Linux-системах. Контейнерное развёртывание возможно, но не является основным сценарием использования.

### Установка инструментов IaC (для шаблонов)

```bash
# Установить Terraform
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform

# Установить Ansible
sudo apt install ansible -y

# Установить k3d (для Kubernetes шаблонов)
wget -q -O - https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash
```

## Настройка

### Конфигурация тарифов на электроэнергию

Создайте `config/electricity.yaml` из примера:

```yaml
# Валюта для расчёта затрат
currency: EUR

# Базовая цена электроэнергии за киловатт-час
price_per_kwh: 0.22

# Опционально: тарификация по времени использования (пиковые/непиковые тарифы)
periods:
  - name: peak
    hours: ["08:00-22:00"]
    price_per_kwh: 0.26
  - name: offpeak
    hours: ["22:00-08:00"]
    price_per_kwh: 0.18
```

**Параметры конфигурации:**
- `currency` — код валюты для отчётов (EUR, USD, RUB и т.д.)
- `price_per_kwh` — цена электроэнергии по умолчанию за кВт⋅ч
- `periods` — опциональная тарификация по времени для пиковых/непиковых часов

### Конфигурация поведения оптимизатора

Создайте `config/optimizer.yaml` из примера:

```yaml
# Профили энергопотребления для различных типов оборудования
power_profiles:
  default:
    base_idle_watts: 65       # Энергопотребление в простое
    watts_per_cpu_core: 12    # Дополнительные ватты на ядро CPU
    watts_per_gb_ram: 0.9     # Дополнительные ватты на GB RAM
  
  low_power_node:
    base_idle_watts: 25
    watts_per_cpu_core: 7
    watts_per_gb_ram: 0.4

# Параметры сценариев консолидации
scenarios:
  consolidate-low-util:
    cpu_threshold: 0.25       # Консолидировать если CPU < 25%
    ram_threshold: 0.30       # Консолидировать если RAM < 30%
    max_node_utilization: 0.70  # Не превышать 70% после консолидации
  
  rightsize:
    cpu_headroom: 0.15        # Оставить 15% резерв CPU
    ram_headroom: 0.20        # Оставить 20% резерв RAM

# Настройки отчётности
reporting:
  markdown_template: default
  enable_ai: false            # Установить в true для отчётов с ИИ
```

### Переменные окружения

```bash
# Опционально: API-ключ OpenAI для ИИ-функций
export OPENAI_API_KEY="sk-..."

# Опционально: учётные данные Proxmox (альтернатива аргументам командной строки)
export PROXMOX_URL="https://pve.example.local:8006"
export PROXMOX_TOKEN_ID="user@pam!tokenname"
export PROXMOX_TOKEN_SECRET="..."
```

## Использование и типовые задачи

### Сбор метрик с инфраструктуры

**Из Proxmox:**
```bash
homelab-cost-optimizer collect \
  --source proxmox \
  --url https://pve.example.local:8006 \
  --token-id user@pam!tokenname \
  --token-secret ВАШ_СЕКРЕТ \
  --out data/proxmox-inventory.json
```

**Из Docker:**
```bash
homelab-cost-optimizer collect \
  --source docker \
  --socket /var/run/docker.sock \
  --out data/docker-inventory.json
```

**Из Kubernetes:**
```bash
homelab-cost-optimizer collect \
  --source kubernetes \
  --kubeconfig ~/.kube/config \
  --out data/k8s-inventory.json
```

### Анализ затрат

```bash
# Создать отчёт анализа затрат
homelab-cost-optimizer analyze \
  --input data/inventory.json \
  --config config/electricity.yaml \
  --out reports/monthly-cost-analysis.md

# Просмотреть отчёт
cat reports/monthly-cost-analysis.md
```

### Получить предложения по консолидации

```bash
# Базовые предложения по консолидации
homelab-cost-optimizer suggest \
  --scenario consolidate-low-util \
  --input data/inventory.json \
  --config config/optimizer.yaml

# Рекомендации на основе ИИ (требуется API-ключ OpenAI)
export OPENAI_API_KEY="sk-..."
homelab-cost-optimizer suggest \
  --scenario consolidate-low-util \
  --input data/inventory.json \
  --config config/optimizer.yaml \
  --ai-report
```

### Развёртывание инфраструктурных шаблонов

**Пример: стек домашней лаборатории Proxmox**

```bash
cd blueprints/terraform/proxmox-homelab

# Инициализировать Terraform
terraform init

# Просмотреть планируемые изменения
terraform plan -var-file="vars/production.tfvars"

# Применить инфраструктуру
terraform apply -var-file="vars/production.tfvars"

# Настроить с помощью Ansible
cd ../../ansible
ansible-playbook -i inventory/homelab playbooks/configure-proxmox.yml
```

## Обновление

### Обновление инструмента оптимизации

```bash
# Перейти в репозиторий
cd homelab-cost-optimizer

# Получить последние изменения
git pull origin main

# Активировать виртуальное окружение
source venv/bin/activate

# Переустановить пакет
pip install -e . --upgrade

# Проверить версию
homelab-cost-optimizer --version
```

### Обновление инфраструктурных шаблонов

```bash
# Получить последние обновления шаблонов
git pull origin main

# Просмотреть изменения в blueprints/
git log --oneline blueprints/

# Пересмотреть план Terraform для оценки изменений инфраструктуры
cd blueprints/terraform/<имя-шаблона>
terraform plan

# Применить обновления осторожно
terraform apply
```

## Логи и устранение неполадок

### Логи приложения

Оптимизатор выводит логи в stdout/stderr по умолчанию:

```bash
# Включить подробное логирование
homelab-cost-optimizer collect --source proxmox ... --verbose

# Перенаправить логи в файл
homelab-cost-optimizer analyze ... 2>&1 | tee optimizer.log
```

### Типовые проблемы и решения

**Проблема: `ModuleNotFoundError: No module named 'homelab_cost_optimizer'`**

```bash
# Решение: установить пакет в виртуальном окружении
source venv/bin/activate
pip install -e .
```

**Проблема: отказ в соединении или таймаут Proxmox API**

```bash
# Проверить сетевое подключение
ping pve.example.local

# Проверить доступность Proxmox API
curl -k https://pve.example.local:8006/api2/json/version

# Проверить правила файрвола, разрешающие порт 8006
sudo ufw status
```

**Проблема: отказ в доступе к сокету Docker**

```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Или запустить с sudo (не рекомендуется)
sudo homelab-cost-optimizer collect --source docker ...
```

**Проблема: нет данных в отчётах о затратах**

```bash
# Проверить, что файл инвентаризации содержит данные
cat data/inventory.json | jq .

# Проверить загрузку конфигурации электричества
homelab-cost-optimizer analyze ... --verbose
```

**Проблема: ИИ-функции не работают**

```bash
# Проверить установку API-ключа OpenAI
echo $OPENAI_API_KEY

# Установить ИИ-зависимости
pip install -e .[ai]

# Проверить подключение к OpenAI API
curl https://api.openai.com/v1/models -H "Authorization: Bearer $OPENAI_API_KEY"
```

## Замечания по безопасности

### Контроль доступа

- 🔒 **Изменить учётные данные по умолчанию** — не используйте стандартные пароли для доступа к Proxmox, Docker или Kubernetes
- 🔐 **Использовать API-токены** — предпочитайте токены аутентификации вместо имени пользователя/пароля
- 🚫 **Ограничить доступ к API** — настройте правила файрвола для ограничения доступа к API-эндпоинтам из доверенных сетей
- 🔑 **Ротация учётных данных** — регулярно обновляйте API-токены и ключи доступа

### Сетевая безопасность

- 🛡️ **Использовать VPN или SSH-туннели** — доступ к API инфраструктуры через зашифрованные соединения
- 🌐 **Не размещать API публично** — держите Proxmox, libvirt и Docker API во внутренних сетях
- 🔥 **Настроить файрволы** — используйте UFW, iptables или сетевые файрволы для ограничения доступа

### Конфиденциальность данных

- 📊 **Данные инвентаризации** — генерируемые JSON-файлы могут содержать детали инфраструктуры; храните их безопасно
- 🗑️ **Очистка отчётов** — удаляйте старые отчёты, содержащие чувствительную информацию об инфраструктуре
- 🔒 **API-ключи** — никогда не фиксируйте API-ключи или токены в системе контроля версий

### Соответствие нормативным требованиям

- ✅ **Работа только с авторизованной инфраструктурой** — собирайте метрики только с систем, которыми вы владеете или управляете
- 📜 **Соблюдение условий API** — следуйте политикам использования Proxmox, Docker и Kubernetes
- ⚖️ **Финансовые оценки носят информационный характер** — расчёты затрат являются приблизительными, а не биллинговыми гарантиями

## Структура проекта

```
homelab-cost-optimizer/
├── blueprints/              # IaC шаблоны и автоматизация
│   ├── terraform/           # Модули разворачивания инфраструктуры
│   ├── ansible/             # Плейбуки управления конфигурацией
│   ├── k3d/                 # Среды разработки Kubernetes
│   └── ai/                  # Помощники шаблонов на базе ИИ
├── optimizer/               # Python пакет (оптимизатор затрат)
│   └── homelab_cost_optimizer/
│       ├── cli.py           # Интерфейс командной строки
│       ├── collectors/      # Сбор данных из API
│       ├── estimators/      # Расчёты мощности и затрат
│       ├── consolidators/   # Алгоритмы оптимизации
│       └── reporters/       # Генерация отчётов
├── ai_providers/            # Слой интеграции ИИ
│   ├── base.py              # Абстрактный интерфейс ИИ-провайдера
│   ├── openai_provider.py  # Реализация OpenAI
│   └── mock_provider.py    # Детерминированный мок для тестирования
├── config/                  # Примеры конфигураций
│   ├── electricity.example.yaml
│   └── optimizer.example.yaml
├── tests/                   # Наборы тестов
│   ├── unit/                # Модульные тесты
│   └── integration/         # Интеграционные тесты
├── scripts/                 # Помощники разработки и CI
│   ├── lint.sh              # Проверка качества кода
│   ├── security_scan.sh     # Аудиты безопасности
│   └── perf_check.sh        # Бенчмарки производительности
├── .github/workflows/       # CI/CD пайплайны
├── LEGAL.md                 # Юридические и нормативные примечания
├── CONTRIBUTING.md          # Руководство по внесению вклада
├── CHANGELOG.md             # История версий
└── LICENSE                  # Лицензия Apache-2.0
```

## Планы развития

**Планируемые функции** (ждём вклада):

- 🌍 **Поддержка дополнительных платформ** — коллекторы для VMware ESXi, OpenStack, LXC/LXD
- 📊 **Интеграция с Grafana** — мониторинг затрат в реальном времени
- 🔄 **Автоматическая консолидация** — автоматическое выполнение рекомендаций по оптимизации
- 💾 **Исторический учёт** — база данных для анализа трендов затрат
- 🌡️ **Мониторинг температуры** — корреляция энергопотребления с термическими данными
- ☁️ **Сравнение с облачными затратами** — сравнение цен on-premise и облачных провайдеров

**Долгосрочное видение:**

- Полные шаблоны observability-стеков
- Оркестрация multi-site домашних лабораторий
- Оценка и бенчмарки энергоэффективности

## Как внести вклад

Вклад приветствуется и ценится! Следуйте этим рекомендациям:

### Как внести вклад

**1. Открыть issue** — обсудите баги, запросы на функции или идеи шаблонов в [GitHub Issues](https://github.com/ranas-mukminov/homelab-cost-optimizer/issues)

**2. Форк и ветка** — создайте ветку функции из `main`:
```bash
git checkout -b feature/название-вашей-функции
```

**3. Следуйте стилю кода:**
- Python: соответствие PEP 8, type hints, длина строки 100 символов
- Используйте форматтер Black: `black .`
- Используйте isort для импортов: `isort .`
- Terraform: `terraform fmt`
- Ansible: `ansible-lint`

**4. Добавьте тесты** — новые функции требуют модульных и интеграционных тестов где применимо:
```bash
pytest tests/
```

**5. Запустите проверки качества:**
```bash
# Линтинг и форматирование
scripts/lint.sh

# Сканирование безопасности
scripts/security_scan.sh

# Проверка производительности
scripts/perf_check.sh
```

**6. Отправьте pull request** — предоставьте чёткое описание изменений и мотивации

### Вклад в шаблоны

При внесении новых IaC-шаблонов:

- Включите Terraform и Ansible компоненты
- Документируйте все переменные и предположения
- Предоставьте примеры файлов `tfvars` и inventory
- Добавьте README в директорию шаблона
- Протестируйте на свежих установках

### Процесс code review

- Все PR требуют прохождения CI-проверок
- Мейнтейнеры рассматривают в течение 3-5 рабочих дней
- Учитывайте обратную связь перед слиянием

## Лицензия

Этот проект лицензирован по **Apache License 2.0** — смотрите файл [LICENSE](LICENSE) для подробностей.

**Краткое содержание:**
- ✅ Свободное использование, изменение и распространение
- ✅ Может использоваться коммерчески
- ✅ Включает патентный грант
- ⚠️ Необходимо включать лицензию и уведомления об авторских правах
- ⚠️ Изменения должны быть документированы

## Автор и коммерческая поддержка

**Автор:** [Ranas Mukminov](https://github.com/ranas-mukminov)

**Профессиональные DevOps и инфраструктурные услуги:** [run-as-daemon.ru](https://run-as-daemon.ru)

### Услуги коммерческой поддержки

Для продакшн-развёртываний, индивидуальных решений и профессиональной помощи:

- 🏗️ **Проектирование и внедрение инфраструктуры** — архитектура инфраструктуры домашних лабораторий и малого бизнеса под ключ
- ☸️ **Kubernetes и оркестрация** — настройка K3s/K8s кластеров, CI/CD пайплайны, observability стеки
- 💰 **Консалтинг по оптимизации затрат** — глубокий анализ и оптимизация расходов на вашу инфраструктуру
- 📊 **Мониторинг и observability** — внедрение и настройка Prometheus, Grafana, Loki
- 🎓 **Обучение и воркшопы** — обучение команды IaC, DevOps-практикам и управлению затратами
- 🔒 **Аудиты безопасности** — проверка безопасности инфраструктуры и её укрепление

**Контакты:** [run-as-daemon.ru](https://run-as-daemon.ru) или через [GitHub профиль](https://github.com/ranas-mukminov)

---

**Сделано с ❤️ для энтузиастов домашних лабораторий и команд малого бизнеса**

**Профессиональная поддержка:** [run-as-daemon.ru](https://run-as-daemon.ru)
